# Projects

User projects are generated here by `npm run project:init`.

The public harness repo ignores:

- `projects/active/`
- `projects/inactive/`
- `projects/registry.json`

That keeps manuscripts, logs, exports, and review artifacts out of the reusable
tool repository by default.

